{
    "scripts": {
        "dev": "netlify-lambda serve resources/api",
        "build": "netlify-lambda build resources/api"
    },
    "devDependencies": {
        "netlify-lambda": "^2.0.16"
    }
}
